/**
 *
 * Reldens - Editor Constants
 *
 */

const EDITOR_PATH = '/editor';
const ASSET_BROWSER_PATH = '/editor/asset-browser';
const DEFAULT_MAP_NAME = 'untitled';
const DEFAULT_MAP_WIDTH = 24;
const DEFAULT_MAP_HEIGHT = 24;
const DEFAULT_TILE_SIZE = 32;
const MAP_EXTENSION = '.json';
const TILESET_IMAGE_EXTS = new Set(['.png', '.jpg', '.jpeg', '.gif', '.webp', '.bmp']);
const MAX_MAP_SIZE = 512;

module.exports = {
    EDITOR_PATH,
    ASSET_BROWSER_PATH,
    DEFAULT_MAP_NAME,
    DEFAULT_MAP_WIDTH,
    DEFAULT_MAP_HEIGHT,
    DEFAULT_TILE_SIZE,
    MAP_EXTENSION,
    TILESET_IMAGE_EXTS,
    MAX_MAP_SIZE
};
